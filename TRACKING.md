# Build tracking — salesforcedx-vscode-manager

A running ledger of AI-assisted work: **tokens used** per task, **lines of code**
generated/deleted, and any notes worth remembering. Update the "Summary" block
at the bottom whenever a row is added.

> **How to read the LoC columns.** Numbers are `git log --numstat` sums across
> all commits belonging to a task, **excluding `package-lock.json`** (which
> dominates Phase 1 otherwise). `net = added − deleted`.
>
> **How to read the Tokens column.** Only the Phase 6 subagent reported its
> own token usage directly (73,214 via the task-notification). Main-session
> phases show `—` here; fill them in from your harness telemetry / transcript
> inspection when available.

## Per-task ledger

| Task | Duration | Commits | LoC added | LoC deleted | Net | Tokens | Notes |
|---|---|---|---|---|---|---|---|
| **Planning** (explore IDEx + AFV, design, incorporate VSIX + status-bar + tracker idea) | ~35 min | 1 (initial `PLAN.md`) | 448 | 0 | 448 | — | Three parallel Explore agents; one Plan agent; four rounds of AskUserQuestion. |
| **Phase 0 — Bootstrap** | ~5 min | 2 | 452 | 4 | 448 | — | `.gitignore`, `LICENSE`, `CHANGELOG`, `README`, `PLAN.md`. gpg passphrase blocker resolved by user. |
| **Phase 1 — Build & test scaffolding** | ~4 min | 1 | 656 | 28 | 628 | — | `package.json`, `tsconfig.json`, `esbuild.js`, `jest.config.js`, `eslint.config.mjs`, CI workflow, vscode mock, first passing test. |
| **Phase 2 — Activation skeleton** | ~6 min | 2 (incl. icon fix) | 424 | 17 | 407 | — | Logger, four empty service wrappers, activity-bar container, two placeholder tree views. Icon SVG had to be rewritten (stroke → filled paths). |
| **Phase 3 — Extension service** | ~5 min | 1 | 264 | 7 | 257 | — | `managed()` / `isInstalled()` / `isEnabled()` / `readManifest()` / `enable` / `disable` / `install` / `uninstall`. 12 unit tests. |
| **Phase 4 — Groups model** | ~4 min | 1 | 348 | 4 | 344 | — | Built-in Apex/Lightning/React-stub, merge-store, applier. 15 unit tests. |
| **Phase 5 — Group commands + tree view** (+ enablement debugging + code-CLI pivot) | ~35 min | 4 (feat, icon/toast fix, diag logging, code-CLI pivot) | 519 | 82 | 437 | — | Initial implementation used `workbench.extensions.action.*` commands that don't exist in current VSCode → pivoted to `code --install/--uninstall` as the apply mechanism. Big design shift; captured as a backend setting. |
| **Phase 6 — Dependency engine** | ~5 min of main + ~5 min of subagent | 2 (subagent commit + merge) | 794 | 7 | 787 | **73,214** (subagent) | Ran in a sibling git worktree via background subagent; subagent could not commit due to sandbox, main session committed and merged. 65 new tests. |
| **Phase 7 — Dependency commands + tree view** | ~5 min | 1 | 383 | 23 | 360 | — | Category grouping, status icons with ThemeColor, markdown report formatter. 7 unit tests. |
| **Phase 8 — VSIX override** | ~10 min | 1 | 555 | 22 | 533 | — | Scanner (filename parser + FS watcher), installer (`--force`, provenance tracking), 4 commands, groups-tree badges. 11 unit tests. |
| **Phase 9 — Status bar** | ~6 min | 1 | 245 | 8 | 237 | — | Two left-aligned items with warning background when VSIX active; wires through `onAfterApply` callback. 7 unit tests. |
| **Phase 10 — Polish** | ~15 min | 1 | 322 | 20 | 302 | — | Walkthrough (4 steps), README rewrite, CHANGELOG, VSIX packaging (`dist/` output + `.vscodeignore` tuning). Clean 12-file 22 KB `.vsix`. |
| **Docs / TODOs follow-ups** | — | 3 | 74 | 0 | 74 | — | `PLAN.md` §9 TODOs (topological uninstall, id-dedup, installed-version/update indicators, etc.). |
| **Docs: TRACKING + CONTRIBUTING + CLAUDE** | ~15 min | 2 | 285 | 3 | 282 | — | `TRACKING.md` effort ledger, `CONTRIBUTING.md` worktree/gpg recipe, `CLAUDE.md` agent directions. Ran in parallel with two backgrounded subagents. |
| **Feature: Dependencies tree dedup by fingerprint** | ~5 min setup + ~5 min subagent | 2 (subagent commit + merge) | 321 | 24 | 297 | **75,771** (subagent) | Backgrounded subagent in `feat/deps-dedup-fingerprint` worktree. Sandbox blocked the subagent's `git commit`; main session committed in the worktree and merged. Added fingerprint-based folding (built-in > shim > manifest precedence); tooltip lists multiple owners. 93 → 100 tests. |
| **Feature: Groups tree version + update indicators** | ~5 min setup + ~10 min subagent + ~3 min merge | 2 (subagent commit + merge w/ conflict resolution) | ~850 | ~50 | ~800 | **132,302** (subagent) | Backgrounded subagent in `feat/groups-version-indicators` worktree, ran concurrently with the dedup subagent. Same sandbox-blocks-commit pattern; main session committed + merged. Added `MarketplaceVersionService` (1h-cached probe), `ExtensionService.getNodeVersionInfo`, three new commands, one new setting. PLAN.md had a real merge conflict (both branches edited the same TODO block) resolved by keeping both checkboxes. 100 → 124 tests. |
| **Cleanups batch (7 §9 TODOs)** | ~30 min foreground | 1 | 280 | 60 | 220 | — | Two backgrounded subagents died on Bash-permission denial before writing any files (harder sandbox than prior rounds). Pivoted to foreground. New layers-glyph SVG; `MarketplaceVersionService.resolveExistence()` + cached per-id skip on `'missing'`; `useInternalCommands` setting fully removed; `autoRunDependencyChecks` verified wired; `GroupStore.validateGroup()` + `upsert()` guard against empty user groups; CLAUDE.md audit adds the three subagent-environment failure modes. 124 → 135 tests. |
| **Feature: Extension-dependency-graph awareness** | ~40 min foreground | 1 | 574 | 66 | 508 | — | Same session; shared the foreground-only pivot. `ExtensionService` gains `getDependencyGraph` / `transitiveDependencies` / `computeBlockedByDependents` / `topologicalUninstallOrder`. `applyGroup` rewritten to auto-include transitive deps, refuse to break the graph, and uninstall in topological order. `GroupsTreeProvider` extension nodes become collapsible with dep/pack children. New `reloadAfterApply` setting + consolidated prompt at end of apply. 135 → 148 tests. |
| **Feature: l10n externalization (per AFV pattern)** | ~40 min foreground | 1 | 694 | 156 | 538 | — | `package.json` switched to `%key%` placeholders resolved by new `package.nls.json`. New `src/localization/` module (`LocalizationKeys` enum + `localizationValues` map + `getLocalization()` helper wrapping `vscode.l10n.t`). Swept every show-message / quick-pick / status-bar / tree-item user-facing string. New `npm run l10n` script, seeded `l10n/bundle.l10n.json`, CI-guard test asserting every key has a default. `CONTRIBUTING.md` gets a Translating section; `CLAUDE.md` adds the externalized-strings rule. 148 → 151 tests. |
| **Feature: Phase 11 niceties + checkForUpdates wiring** | ~35 min foreground | 1 | 790 | 48 | 742 | — | Import/export groups (new `groupIO.ts` with versioned payload + conflict resolution), workspace-scoped groups (`getGroupsByScope` / `moveToScope` / scope badge in the tree + `Move Group to User/Workspace` command), `Cmd+Alt+G` keybinding, and `checkForUpdates` now also invokes `workbench.extensions.action.checkForUpdates`. Second CLAUDE audit pass adds the foreground-pivot rule and a test-count-delta commit requirement. 152 → 170 tests (+18). |
| **Feature: Auto-discover Salesforce extension-pack groups** | ~15 min foreground | 1 | 262 | 13 | 249 | — | New `src/groups/packGroups.ts` scans `vscode.extensions.all` for any Salesforce-published extension with a non-empty `extensionPack` and surfaces each as a read-only group. Synthetic id `pack:<extensionId>`, distinct `extension pack` badge + `$(package)` icon. `Group.source` (`'code' \| 'pack' \| 'user'`) drives tree rendering. `remove` / `moveToScope` throw on pack ids. Menu visibility gates Edit/Delete/Move away from pack entries. 170 → 179 tests (+9). |
| **Feature: Salesforce marketplace catalog integration** | ~40 min foreground | 1 | ~870 | ~27 | ~840 | — | `MarketplaceVersionService.listPublisherExtensions()` paginates the gallery `extensionquery` API (filterType 10 + `publisher:"..."`) and returns `CatalogEntry[]` sorted by install count. New `PublisherCatalogService` owns the snapshot lifecycle with the `updateCheck` setting as the refresh schedule. `GroupStore` gains an optional catalog read hook; when populated it surfaces an `All Salesforce Extensions` group (id `catalog:salesforce`, `$(cloud)` icon, `marketplace catalog` badge). Two new commands: `Browse Salesforce Extensions...` (multi-select QuickPick with install counts formatted as `12.3k` / `1.2M`) and `Refresh Salesforce Catalog`. View-title `$(cloud)` button on the Groups view. Matches the reference SO body (pageSize 100, sortBy 0, assetTypes [], flags 0x194). 179 → 199 tests (+20). |
| **Feature: Pack built-ins (Salesforce Extension Pack + Expanded)** | ~10 min foreground | 2 (76fca99 + ac1a20e) | 112 | 3 | 109 | — | Hardcoded `Salesforce Extension Pack` and `Salesforce Extension Pack (Expanded)` groups in `builtInGroups.ts`. Then `GroupStore` learned to suppress the handcrafted entries whenever the real pack extension is installed, so the live pack-discovery result wins and we don't show two near-duplicate entries. |
| **Feature: Per-extension install/uninstall + bulk-apply guard** | ~15 min foreground | 1 (91ed9a2) | 243 | 15 | 228 | — | New `Install Extension` / `Uninstall Extension` context-menu actions on every extension tree node (addresses "too many extensions to apply at once"). Bulk `Apply Group` now refuses to run against the synthetic catalog group so the user doesn't accidentally install every Salesforce extension. |
| **Docs: Product announcement** | ~5 min foreground | 1 (a6e77a1) | 76 | 0 | 76 | — | Long-form + highlights markdown under `docs/` ahead of the v0.1 launch post. Purely docs; no code. |
| **Fix: Honor freshly-installed deps during apply** | ~15 min foreground | 1 (b96acc9) | 277 | 4 | 273 | — | `applyGroup` re-snapshots the dep graph after the install pass so deps pulled in by the `code` CLI for a group member aren't treated as "not in the group → disable/uninstall." Repro: installing the Apex group uninstalled `einstein-gpt` because it was a fresh transitive dep VSCode resolved mid-apply and our pre-install graph missed it. `ExtensionService.getDependencyGraph` also learned to augment from `~/.vscode/extensions/<id>-<version>/package.json` to pick up installs that haven't surfaced in `vscode.extensions.all` yet. |
| **Fix: Cascade uninstall through dependents** | ~10 min foreground | 1 (8f34853) | 180 | 22 | 158 | — | `uninstallExtension` now enumerates `transitiveDependents`, prompts once with a modal that lists the cascade, then uninstalls in `topologicalUninstallOrder` so VSCode doesn't reject any step with `Cannot uninstall X. Y depends on this.` Regression fixture in `updateCommands.test.ts`. |
| **Fix: Sticky notifications via notify helper** | ~10 min foreground | 1 (564e155) | 184 | 45 | 139 | — | New `src/util/notify.ts` (`notifyInfo` / `notifyWarn` / `notifyError`) that always attaches an action button so the toast doesn't auto-dismiss before the user sees it — apply/install/uninstall results in particular were dismissing after ~4 s without a trace. Every state-changing notification was swept over to the helper. |
| **UI: Render extensions by displayName** | ~10 min foreground | 1 (021ec45) | 190 | 12 | 178 | — | `ExtensionService.getDisplayName(id)` is now the single source of truth for tree labels; resolves from `ext.packageJSON.displayName`, disk manifest, then the marketplace catalog snapshot, then the raw id. `GroupsTreeProvider.labelForExtension` + a catalog lookup hook surface pretty names everywhere. |
| **UI: Inline 'Open in Marketplace' button** | ~5 min foreground | 1 (02c359e) | 64 | 6 | 58 | — | Every extension tree node gains an `$(link-external)` inline `view/item/context` button that invokes `extension.open <id>`. Works for both installed and uninstalled ids, surfaces the Install button for missing ones. |
| **Fix: Stop firing native check-for-updates modal** | ~5 min foreground | 1 (c6e6587) | 33 | 34 | −1 | — | Our `checkForUpdates` command used to also invoke `workbench.extensions.action.checkForUpdates`, which pops a native modal ("All extensions are up to date." / "Install and Reload") we can't suppress. Users invoking our command wanted our tree to refresh, not the modal — so we stick to our own `MarketplaceVersionService` probe plus a sticky notification. Regression test in `updateCommands.test.ts`. |
| **Feature: Display names in notification copy** | ~25 min foreground | 1 | ~155 | ~30 | ~125 | — | New `ExtensionService.label(id)` centralizes the "display name, with raw id as final fallback" dance and takes over the catalog lookup that used to live on the tree provider, so tree labels AND notification bodies share one resolver. `updateCommands.ts` and `catalogCommands.ts` route every id-bearing toast/modal through `label()` (install / uninstall / update / cascade-confirm). Log lines keep the raw id per policy. 229 → 235 tests (+6). |
| **Feature: Prefix-based VSIX override matching** | ~25 min foreground (plan + implementation) | 1 | ~250 | ~10 | ~240 | — | New `parseVsixFilenameFuzzy(filename, managedIds)` handles CI-renamed artifacts (e.g. `salesforcedx-einstein-gpt-welcome-show-3.28.0.vsix` → `salesforce.salesforcedx-einstein-gpt`) by longest-prefix matching the filename stem against the names of managed extension ids, with a `stem[name.length] in {'-', '.', ''}` boundary guard to prevent false positives (`apex` doesn't match `apexoas`). `VsixOverride` gains an optional `matchedBy: 'strict' \| 'prefix'` tag; `VsixInstaller.tryInstall` logs the mapping when fuzzy fires so unintended matches are discoverable in the output channel. Wired via a new `VsixScanner.setManagedIdLookup(fn)` hook in `extension.ts`. 278 → 287 tests (+9, six on `parseVsixFilenameFuzzy`, two on `VsixScanner.scan` integration, one on the installer log line). |
| **Feature: Auto-run dep check after apply + Salesforce CLI update indicator** | ~35 min foreground | 2 (auto-run first, then CLI indicator) | ~300 | ~15 | ~285 | — | Two related pieces split into separate commits. (1) `runApply` in `groupCommands` fires `sfdxManager.runDependencyCheck` when the apply touched state; no-op re-applies skip it. New `groupCommands.test.ts` asserts both paths. (2) New `src/services/cliVersionService.ts` probes `developer.salesforce.com/media/salesforce-cli/sf/channels/stable/version` with a 1 h cache + `AbortController`-gated timeout; returns `undefined` on offline / non-OK / missing-version. `DependenciesTreeProvider` gains `setCliLatestVersion(v)` and renders an `$(arrow-circle-up)` + `update → v<latest>` description on the `builtin.sf-cli` row only when the check passed AND we have both versions AND latest > installed. Gated by the existing `updateCheck` setting; `never` suppresses the probe. Two new localization keys. 296 → 308 tests (+12: 2 post-apply, 6 CliVersionService, 4 tree-badge paths). |
| **Refactor: Drop manager-specific telemetry opt-out** | ~10 min foreground | 1 | ~25 | ~75 | -50 | — | The shared `@salesforce/vscode-service-provider` telemetry pipeline already honors `telemetry.telemetryLevel` + Salesforce-side enablement. Removed the duplicate `salesforcedx-vscode-manager.telemetry.enabled` setting, its nls entry, the `SETTINGS.telemetryEnabled` constant, the `TelemetryService.enabled` static + `refreshEnabled` helper + `readSettingEnabled`, and the `onDidChange` branch that recomputed it. Also dropped the two telemetry tests that exercised the setting path. 298 → 296 tests (-2 as expected). Docs (`telemetry-events.md`, `smoke-tests.md`) updated so the "Opt-out" story points at VSCode's global telemetry level instead. |
| **Feature: Authoritative VSIX overrides + dedicated view** | ~60 min foreground (plan + implementation) | 1 | ~750 | ~60 | ~690 | — | Major UX shift: the override directory is now the source of truth for matched ids. New `VsixInstaller.autoInstallAll()` force-installs every `.vsix` at activation and on every watcher event (idempotent — skips rows already at the matching version). Groups tree appends `:vsixLocked` to the row's `contextValue` whenever `vsixFilename` is set, and 9 `package.json` menu entries (install/update/uninstall across inline + context menu + view/title anchors) append `!(viewItem =~ /:vsixLocked/)` so those row actions disappear. New `src/views/vsixTreeProvider.ts` renders a third top-level tree under the activity-bar container, gated by a new `sfdxManager.hasVsixOverrides` context key (toggled from the scanner watcher). Two new commands `sfdxManager.removeVsixOverride` / `sfdxManager.revealVsixFile`. New setting `vsixAutoInstall` (default `true`, replaces the dead `vsixAutoReinstallOnChange`). CLAUDE.md gets a "VSIX overrides are authoritative" rule. 289 → 298 tests (+9: three `autoInstallAll` cases, three `vsixTreeProvider` cases, one `:vsixLocked` contextValue assertion, two new `viewsWelcome` contract tests). |
| **Fix: VSIX prefix lookup includes uninstalled ids + "vsix available" row badge** | ~15 min foreground | 1 | ~90 | ~5 | ~85 | — | Follow-up after a user report that dropping a VSIX for an uninstalled extension showed nothing. Root cause: `extensions.managed()` only returns installed ids, so the scanner's lookup was too narrow. Widened the id lookup in `extension.ts` to the union of installed `managed()` ids, every group-member id from `store.list()` (built-in / user / pack / catalog), and the publisher catalog snapshot — so any id the manager knows about is matchable. Second fix for discoverability: the tree provider now shows a `vsix available` badge on uninstalled rows whose override is waiting, with the filename in the tooltip. Two new localization keys surface the signal. 287 → 289 tests (+2: uninstalled-id scan case, tree-provider badge render). |
| **Feature: Friendly viewsWelcome copy** | ~15 min foreground | 2 (TODO commit + feature) | ~85 | ~1 | ~84 | — | Replaces VSCode's built-in *"There is no data provider registered…"* placeholder on cold start. New `contributes.viewsWelcome` entries for both `sfdxManager.groups` and `sfdxManager.dependencies`, each sourcing a markdown blurb + `command:` links from new `package.nls.json` keys. New `test/unit/viewsWelcome.test.ts` (3 cases) asserts every view has a welcome entry, every welcome `contents` is a resolvable nls key, and every `command:<id>` link references a registered command. 275 → 278 tests. Followup `onView:*` activation trade-off still open. |
| **Feature: Row spinner + Groups-panel freeze** | ~45 min foreground (plan mode + implementation) | 1 | ~400 | ~70 | ~330 | — | New `src/util/busyState.ts` refcounts in-flight ids, broadcasts the `sfdxManager.anyBusy` VSCode context key via `setContext`, and fires an `onChange` event the tree and status-bar items subscribe to. Every mutating command in `updateCommands` / `groupCommands` / `catalogCommands` / `vsixCommands` wraps its body in `busy.withBusy([...ids], fn)` via a per-file `withBusy` helper; cascade uninstall flags every victim so the whole chain spins, apply-group flags a `__group_apply__:<id>` sentinel plus group members plus all managed ids. `GroupsTreeProvider.resolveIcon` returns `ThemeIcon('sync~spin')` when `busy.isBusy(id)` — spinner wins over every other icon state. Both status-bar items prefix their text with `$(sync~spin)` on change. 17 menu `when` clauses in `package.json` (all `view/title` + `view/item/context` targeting `sfdxManager.groups`) now append `&& !sfdxManager.anyBusy`. New `busyState.test.ts` (7 cases) + spinner / cascade-busy / throw-releases cases on tree-provider + update-commands suites. 262 → 275 tests (+13). |
| **Feature: Telemetry + locked-extension UX** | ~60 min foreground (plan mode + implementation) | 1 | ~650 | ~40 | ~610 | — | Hard dep on `salesforcedx-vscode-core` via `extensionDependencies`. New `TelemetryService` singleton in `src/services/telemetryService.ts` pulls the reporter via `@salesforce/vscode-service-provider`'s `ServiceProvider.getService(ServiceType.Telemetry, ...)` (AFV pattern; same call site as `salesforcedx-vscode-einstein-gpt`). Typed emit helpers so feature code can't typo event names; all no-op when the reporter is missing or the user sets `telemetry.enabled=false`. Event surface: activation/deactivation, group.apply (counts by category), extension.install/uninstall/update, catalog.refresh, dependency.check, error. New `ExtensionService.isLocked(id)` BFS-expands the manager's own `extensionDependencies` chain; the tree marks locked rows with a `:locked` contextValue suffix + `required` badge, the view/item/context menu hides install/uninstall for them, `uninstallExtension` early-returns with a sticky info toast as defense-in-depth, and `applyGroup` / `disableAllSalesforce` filter locked ids out of the candidate-disable set. New setting `salesforcedx-vscode-manager.telemetry.enabled` (default `true`) for an extension-specific kill switch on top of the global `telemetry.telemetryLevel`. `engines.vscode` floor bumped `^1.86.0` → `^1.90.0` to match core. 235 → 253 tests (+18, new telemetryService.test.ts + isLocked + locked-enforcement cases across updateCommands/groupApplier/groupsTreeProvider). |

## Summary (keep up to date)

- **Total repository** (all non-merge commits, excluding `package-lock.json`): see `git log --numstat` for the authoritative count; current run shows **+12,732 / −1,114 (net +11,618)** after telemetry + locked-extensions.
- **Total commits**: 62 on `main` (including 3 merges).
- **Tests**: **308 passing** across 26 suites.
- **Packaged VSIX**: 19 files, ~49 KB (size includes the new `@salesforce/vscode-service-provider` wrapper).
- **Known token usage**: 73,214 (Phase 6) + 75,771 (dedup) + 132,302 (versions/updates) = **281,287 measured across the three successful subagents**. Two additional subagents (dep-graph-cluster + cleanups) were launched but died on Bash denial before doing any work; their tokens (13,004 + 13,555 = 26,559) produced no output.
- **Phases complete**: 0 through 11 + pack-groups discovery + marketplace catalog + per-extension lifecycle + cascade-uninstall + displayName + sticky notifications + display-names-in-notifications + **notification audit** + **telemetry + locked-extension UX**. The telemetry backlog item is closed; only the four manual F5 smoke tests remain.
- **Open follow-ups**: See `PLAN.md` §9 — the four manual F5 smoke tests (captured as executable checklists in `docs/smoke-tests.md` and `docs/notification-verification.md`).
