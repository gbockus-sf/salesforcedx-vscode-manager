import * as vscode from 'vscode';

export const activate = (_context: vscode.ExtensionContext): void => {
  // Phase 1 placeholder — real activation wiring lands in Phase 2.
};

export const deactivate = (): void => {
  // no-op
};
